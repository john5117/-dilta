/**
 * entity name mapping to avoid magical variables
 *
 * @export
 * @enum {number}
 */
export enum EntityNames {
  User = 'user',
  Manager = 'manager',
  School = 'school',
  Auth = 'auth',
  Parent = 'parent',
  Score = 'score',
  Student = 'student',
  Receipt = 'receipt'
}
