import { OfflinedatabaseModule } from './offlinedatabase.module';

describe('OfflinedatabaseModule', () => {
  it('should work', () => {
    expect(new OfflinedatabaseModule()).toBeDefined();
  });
});
